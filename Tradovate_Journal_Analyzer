<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Tradovate Journal Analyzer</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;500;600;700&family=IBM+Plex+Mono:wght@400;500;600&display=swap" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <style>
    :root,[data-theme="light"]{--color-bg:#f7f6f2;--color-surface:#f9f8f5;--color-surface-2:#fbfbf9;--color-surface-offset:#f3f0ec;--color-text:#28251d;--color-text-muted:#7a7974;--color-text-inverse:#f9f8f4;--color-primary:#01696f;--color-primary-hover:#0c4e54;--color-success:#437a22;--color-error:#a13544;--color-warning:#964219;--radius-md:.5rem;--radius-lg:.75rem;--radius-xl:1rem;--radius-full:9999px;--shadow-sm:0 1px 2px rgba(20,18,14,.06);--shadow-md:0 4px 12px rgba(20,18,14,.08);--font-body:'IBM Plex Sans',sans-serif;--font-mono:'IBM Plex Mono',monospace;--text-xs:clamp(.75rem,.7rem + .25vw,.875rem);--text-sm:clamp(.875rem,.8rem + .35vw,1rem);--text-base:clamp(1rem,.95rem + .25vw,1.125rem);--text-lg:clamp(1.125rem,1rem + .75vw,1.5rem);--text-xl:clamp(1.5rem,1.2rem + 1.25vw,2rem);--space-2:.5rem;--space-3:.75rem;--space-4:1rem;--space-5:1.25rem;--space-6:1.5rem;--space-8:2rem;--space-12:3rem;--space-16:4rem;}
    [data-theme="dark"]{--color-bg:#111315;--color-surface:#171a1d;--color-surface-2:#1c2024;--color-surface-offset:#14181b;--color-text:#e5e8eb;--color-text-muted:#98a2ad;--color-text-inverse:#0d0f11;--color-primary:#4f98a3;--color-primary-hover:#67aab4;--color-success:#69b36d;--color-error:#db6f7c;--color-warning:#d09b58;--shadow-sm:0 1px 2px rgba(0,0,0,.2);--shadow-md:0 4px 12px rgba(0,0,0,.3)}
    *,*::before,*::after{box-sizing:border-box;margin:0;padding:0} body{min-height:100vh;font-family:var(--font-body);font-size:var(--text-base);line-height:1.55;color:var(--color-text);background:radial-gradient(circle at top right,rgba(79,152,163,.08),transparent 28%),linear-gradient(180deg,var(--color-bg),var(--color-surface-offset))} button,input,select,textarea{font:inherit;color:inherit} button{cursor:pointer;border:none} .app-shell{max-width:1280px;margin:0 auto;padding:var(--space-6);display:grid;gap:var(--space-6)} .topbar{display:flex;flex-wrap:wrap;justify-content:space-between;gap:var(--space-4);align-items:center;padding:var(--space-5);border:1px solid rgba(255,255,255,.08);background:rgba(23,26,29,.82);backdrop-filter:blur(14px);border-radius:var(--radius-xl);box-shadow:var(--shadow-md);position:sticky;top:var(--space-4);z-index:10} .brand{display:flex;align-items:center;gap:var(--space-4)} .logo{width:44px;height:44px;border-radius:12px;display:grid;place-items:center;background:linear-gradient(135deg,rgba(79,152,163,.18),rgba(79,152,163,.04));border:1px solid rgba(79,152,163,.25);color:var(--color-primary);flex-shrink:0} .logo svg{width:24px;height:24px} .brand h1{font-size:var(--text-xl);line-height:1.05;letter-spacing:-.03em} .brand p{color:var(--color-text-muted);font-size:var(--text-sm);margin-top:4px} .topbar-actions{display:flex;gap:var(--space-3);flex-wrap:wrap} .btn{display:inline-flex;align-items:center;gap:var(--space-2);min-height:44px;padding:0 var(--space-4);border-radius:var(--radius-md);font-size:var(--text-sm);font-weight:600;border:1px solid transparent} .btn-primary{background:var(--color-primary);color:var(--color-text-inverse)} .btn-secondary{background:var(--color-surface-2);border-color:rgba(255,255,255,.08);color:var(--color-text)} .dashboard-grid{display:grid;grid-template-columns:360px 1fr;gap:var(--space-6);align-items:start} .panel,.kpi,.chart-card,.table-card,.summary-card{background:rgba(23,26,29,.86);border:1px solid rgba(255,255,255,.08);border-radius:var(--radius-xl);box-shadow:var(--shadow-sm);overflow:hidden} .sidebar{display:grid;gap:var(--space-6);position:sticky;top:108px} .panel-header{padding:var(--space-5);border-bottom:1px solid rgba(255,255,255,.08)} .panel-header h2{font-size:var(--text-lg);line-height:1.1;margin-bottom:4px} .panel-header p{color:var(--color-text-muted);font-size:var(--text-sm)} .panel-body{padding:var(--space-5)} .upload-zone{border:1.5px dashed rgba(79,152,163,.4);border-radius:var(--radius-lg);padding:var(--space-6);display:grid;gap:var(--space-4);background:rgba(79,152,163,.06)} .upload-zone input[type="file"]{width:100%;padding:var(--space-3);background:var(--color-surface-2);border:1px solid rgba(255,255,255,.08);border-radius:var(--radius-md)} .field-grid{display:grid;gap:var(--space-4);margin-top:var(--space-5)} .field label{display:block;font-size:var(--text-xs);font-weight:600;text-transform:uppercase;letter-spacing:.08em;color:var(--color-text-muted);margin-bottom:var(--space-2)} .field input,.field select,.field textarea{width:100%;min-height:44px;padding:0 var(--space-3);border-radius:var(--radius-md);background:var(--color-surface-2);border:1px solid rgba(255,255,255,.08);outline:none} .field textarea{min-height:100px;padding-top:var(--space-3);resize:vertical} .hint{margin-top:var(--space-2);color:var(--color-text-muted);font-size:var(--text-xs)} .status-row{display:flex;flex-wrap:wrap;gap:var(--space-3);margin-top:var(--space-4)} .chip{display:inline-flex;align-items:center;min-height:36px;padding:0 var(--space-3);border-radius:var(--radius-full);background:var(--color-surface-2);border:1px solid rgba(255,255,255,.08);color:var(--color-text-muted);font-size:var(--text-xs);font-weight:600;letter-spacing:.05em;text-transform:uppercase} .main{display:grid;gap:var(--space-6)} .kpi-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:var(--space-4)} .kpi{padding:var(--space-5)} .kpi .label{color:var(--color-text-muted);font-size:var(--text-xs);text-transform:uppercase;letter-spacing:.08em;margin-bottom:var(--space-2)} .kpi .value{font-family:var(--font-mono);font-variant-numeric:tabular-nums lining-nums;font-size:clamp(1.35rem,1.1rem + 1vw,2rem);font-weight:600;letter-spacing:-.03em} .kpi .sub{margin-top:var(--space-2);font-size:var(--text-sm);color:var(--color-text-muted)} .positive{color:var(--color-success)} .negative{color:var(--color-error)} .neutral{color:var(--color-warning)} .content-grid,.double-grid{display:grid;grid-template-columns:1fr 1fr;gap:var(--space-6)} .content-grid{grid-template-columns:1.3fr 1fr} .chart-wrap{padding:var(--space-5);height:320px} .table-wrap{padding:0 var(--space-5) var(--space-5);overflow-x:auto} table{width:100%;border-collapse:collapse;min-width:560px} th,td{text-align:left;padding:.85rem .75rem;border-bottom:1px solid rgba(255,255,255,.08);font-size:var(--text-sm);vertical-align:top} th{color:var(--color-text-muted);font-size:var(--text-xs);text-transform:uppercase;letter-spacing:.08em;font-weight:600} td.num{font-family:var(--font-mono);font-variant-numeric:tabular-nums lining-nums} .summary-card .panel-body{display:grid;gap:var(--space-4)} .bullet{padding:var(--space-4);background:var(--color-surface-2);border:1px solid rgba(255,255,255,.06);border-radius:var(--radius-lg);font-size:var(--text-sm)} .warn{color:var(--color-warning)} .empty-state{padding:var(--space-12);text-align:center;color:var(--color-text-muted)} .footer-note{color:var(--color-text-muted);font-size:var(--text-xs);text-align:center;padding-bottom:var(--space-8)} @media (max-width:1120px){.dashboard-grid,.content-grid,.double-grid,.kpi-grid{grid-template-columns:1fr}.sidebar{position:static}.chart-wrap{height:280px}} @media (max-width:640px){.app-shell{padding:var(--space-4)} .topbar{top:var(--space-2);padding:var(--space-4)} .brand h1{font-size:1.4rem}}
  </style>
</head>
<body>
<div class="app-shell">
<header class="topbar"><div class="brand"><div class="logo"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M4 17 9 12l3 3 8-8"/><path d="M18 7h2v2"/><path d="M4 20h16"/></svg></div><div><h1>Tradovate Journal Analyzer</h1><p>Reads Tradovate-style fill CSVs, pairs fills into trades, and computes the math inside the dashboard.</p></div></div><div class="topbar-actions"><button class="btn btn-secondary" id="themeToggle">Theme</button><button class="btn btn-secondary" id="downloadSampleBtn">Download sample fills</button></div></header>
<div class="dashboard-grid">
<aside class="sidebar">
<section class="panel"><div class="panel-header"><h2>Upload Tradovate CSV</h2><p>Use raw fill/order export. The app pairs fills into round-trip trades and calculates P&amp;L automatically.</p></div><div class="panel-body"><div class="upload-zone"><input type="file" id="csvFileInput" accept=".csv" /><div class="hint">Expected Tradovate-style columns: Date, Fill Time, Contract, B/S, avgPrice, filledQty. Header matching is flexible.</div></div><div class="field-grid"><div class="field"><label for="commissionPerSide">Commission per side, per contract</label><input id="commissionPerSide" type="number" step="0.01" value="0.37" /><div class="hint">Applied to both entry and exit. Example: 0.37 means 0.74 round-trip per contract.</div></div><div class="field"><label for="losingThreshold">Losing cluster minimum trades</label><input id="losingThreshold" type="number" step="1" value="3" /></div><div class="field"><label for="strategyFocus">Review note</label><textarea id="strategyFocus">Use this to identify weak hours, weak setups, and recurring mistakes without reconstructing P&amp;L manually.</textarea></div></div><div class="status-row"><span class="chip" id="fileStatus">No file loaded</span><span class="chip" id="tradeCountStatus">0 paired trades</span><span class="chip" id="unknownStatus">0 unknown specs</span></div></div></section>
<section class="panel"><div class="panel-header"><h2>Built-in specs</h2><p>Uses Tradovate market specs for common futures roots. Unknown roots get flagged for manual point value entry.</p></div><div class="panel-body"><div class="bullet">Formula: <strong>P&amp;L = price difference × quantity × point value − commissions</strong></div><div class="bullet">Long: exit − entry. Short: entry − exit.</div><div class="bullet">Unknown symbols are listed separately so you can patch them once instead of doing manual journal math forever.</div></div></section>
</aside>
<main class="main">
<section class="kpi-grid"><article class="kpi"><div class="label">Net P&amp;L</div><div class="value" id="kpiNet">$0.00</div><div class="sub" id="kpiNetSub">Waiting for data</div></article><article class="kpi"><div class="label">Win Rate</div><div class="value" id="kpiWinRate">0.0%</div><div class="sub" id="kpiWinRateSub">0 winners / 0 trades</div></article><article class="kpi"><div class="label">Expectancy</div><div class="value" id="kpiExpectancy">$0.00</div><div class="sub" id="kpiExpectancySub">Average P&amp;L per paired trade</div></article><article class="kpi"><div class="label">Worst Leak</div><div class="value" id="kpiLeak">None</div><div class="sub" id="kpiLeakSub">Upload data to detect weak blocks</div></article></section>
<section class="content-grid"><article class="chart-card"><div class="panel-header"><h2>Equity curve</h2><p>Cumulative P&amp;L from paired fills.</p></div><div class="chart-wrap"><canvas id="equityChart"></canvas></div></article><article class="summary-card"><div class="panel-header"><h2>Pattern summary</h2><p>Written notes generated from actual paired trade math.</p></div><div class="panel-body" id="summaryBullets"><div class="empty-state">Upload a CSV to generate the readout.</div></div></article></section>
<section class="double-grid"><article class="chart-card"><div class="panel-header"><h2>Hourly edge</h2><p>Best and worst time blocks by paired trade P&amp;L.</p></div><div class="chart-wrap"><canvas id="hourlyChart"></canvas></div></article><article class="chart-card"><div class="panel-header"><h2>Contract contribution</h2><p>Which roots are actually making or losing money.</p></div><div class="chart-wrap"><canvas id="setupChart"></canvas></div></article></section>
<section class="double-grid"><article class="table-card"><div class="panel-header"><h2>By contract</h2><p>Win rate, avg trade, total P&amp;L, and sample size.</p></div><div class="table-wrap" id="setupTableWrap"></div></article><article class="table-card"><div class="panel-header"><h2>By hour / block</h2><p>Useful for finding session leaks and cutoff times.</p></div><div class="table-wrap" id="hourlyTableWrap"></div></article></section>
<section class="double-grid"><article class="table-card"><div class="panel-header"><h2>Unknown specs</h2><p>Contracts the dashboard could not price automatically.</p></div><div class="table-wrap" id="weekdayTableWrap"></div></article><article class="table-card"><div class="panel-header"><h2>Flagged leaks</h2><p>Negative clusters with enough sample size.</p></div><div class="table-wrap" id="leakTableWrap"></div></article></section>
</main></div><div class="footer-note">Built for raw Tradovate fills. Pair once, analyze forever.</div></div>
<script>
const CONTRACT_SPECS={MES:{pointValue:5,tickSize:.25,tickValue:1.25},ES:{pointValue:50,tickSize:.25,tickValue:12.5},MNQ:{pointValue:2,tickSize:.25,tickValue:.5},NQ:{pointValue:20,tickSize:.25,tickValue:5},MGC:{pointValue:10,tickSize:.1,tickValue:1},GC:{pointValue:100,tickSize:.1,tickValue:10},MCL:{pointValue:100,tickSize:.01,tickValue:1},CL:{pointValue:1000,tickSize:.01,tickValue:10},M6E:{pointValue:12500,tickSize:.0001,tickValue:1.25},M6A:{pointValue:10000,tickSize:.0001,tickValue:1},M6B:{pointValue:6250,tickSize:.0001,tickValue:.625},MYM:{pointValue:.5,tickSize:1,tickValue:.5},YM:{pointValue:5,tickSize:1,tickValue:5},RTY:{pointValue:50,tickSize:.1,tickValue:5},M2K:{pointValue:5,tickSize:.1,tickValue:.5},MBT:{pointValue:.1,tickSize:5,tickValue:.5},MET:{pointValue:.1,tickSize:.05,tickValue:.005}};
const sampleCSV=`Date,Fill Time,Contract,B/S,avgPrice,filledQty
2026-04-13,06:12:00,MGCJ6,Buy,3234.1,1
2026-04-13,06:24:00,MGCJ6,Sell,3249.6,1
2026-04-13,07:05:00,MGCJ6,Sell,3252.4,1
2026-04-13,07:18:00,MGCJ6,Buy,3224.62,1
2026-04-14,06:33:00,MGCJ6,Buy,3248.7,1
2026-04-14,06:49:00,MGCJ6,Sell,3217.46,1
2026-04-14,08:11:00,MGCJ6,Sell,3225.1,2
2026-04-14,08:27:00,MGCJ6,Buy,3192.89,2
2026-04-15,07:41:00,MGCJ6,Buy,3200.0,1
2026-04-15,07:53:00,MGCJ6,Sell,3192.14,1
2026-04-16,06:18:00,MGCJ6,Buy,3195.0,1
2026-04-16,06:29:00,MGCJ6,Sell,3219.97,1
2026-04-16,07:22:00,MGCJ6,Sell,3224.5,1
2026-04-16,07:35:00,MGCJ6,Buy,3194.6,1
2026-04-17,08:02:00,MGCJ6,Buy,3210.0,1
2026-04-17,08:19:00,MGCJ6,Sell,3182.14,1`;
const state={fills:[],trades:[],unknownRoots:[],charts:{},theme:window.matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light'};
const els={csvFileInput:document.getElementById('csvFileInput'),commissionPerSide:document.getElementById('commissionPerSide'),losingThreshold:document.getElementById('losingThreshold'),strategyFocus:document.getElementById('strategyFocus'),fileStatus:document.getElementById('fileStatus'),tradeCountStatus:document.getElementById('tradeCountStatus'),unknownStatus:document.getElementById('unknownStatus'),themeToggle:document.getElementById('themeToggle'),downloadSampleBtn:document.getElementById('downloadSampleBtn'),summaryBullets:document.getElementById('summaryBullets'),setupTableWrap:document.getElementById('setupTableWrap'),hourlyTableWrap:document.getElementById('hourlyTableWrap'),weekdayTableWrap:document.getElementById('weekdayTableWrap'),leakTableWrap:document.getElementById('leakTableWrap'),kpiNet:document.getElementById('kpiNet'),kpiNetSub:document.getElementById('kpiNetSub'),kpiWinRate:document.getElementById('kpiWinRate'),kpiWinRateSub:document.getElementById('kpiWinRateSub'),kpiExpectancy:document.getElementById('kpiExpectancy'),kpiExpectancySub:document.getElementById('kpiExpectancySub'),kpiLeak:document.getElementById('kpiLeak'),kpiLeakSub:document.getElementById('kpiLeakSub')};document.documentElement.setAttribute('data-theme',state.theme);
function splitCSVLine(line){const out=[];let cur='',inQuotes=false;for(let i=0;i<line.length;i++){const ch=line[i],next=line[i+1];if(ch==='"'){if(inQuotes&&next==='"'){cur+='"';i++;}else inQuotes=!inQuotes;}else if(ch===','&&!inQuotes){out.push(cur);cur='';}else cur+=ch;}out.push(cur);return out;} function normalizeHeader(h){return h.toLowerCase().trim().replace(/[^a-z0-9]+/g,'_').replace(/^_|_$/g,'');} function parseCSV(text){const lines=text.replace(/
/g,'').trim().split('
').filter(Boolean);if(lines.length<2) throw new Error('CSV needs a header and at least one data row.');const headers=splitCSVLine(lines[0]).map(normalizeHeader);const rows=[];for(let i=1;i<lines.length;i++){const cells=splitCSVLine(lines[i]);if(!cells.some(c=>c.trim()!=='')) continue;const row={};headers.forEach((header,idx)=>row[header]=(cells[idx]||'').trim());rows.push(row);}return rows;}
function findValue(row,names){for(const name of names){if(row[name]!=null&&row[name]!=='') return row[name];} return '';} function parseNumber(v){if(v==null||v==='') return 0;const cleaned=String(v).replace(/[$,\s]/g,'');const n=Number(cleaned);return Number.isFinite(n)?n:0;} function parseDateTime(row){const dateRaw=findValue(row,['date','fill_date','trade_date']);const timeRaw=findValue(row,['fill_time','time','timestamp'])||'00:00:00';const iso=`${dateRaw}T${timeRaw.length===5?timeRaw+':00':timeRaw}`;const dt=new Date(iso);return Number.isNaN(dt.getTime())?new Date(dateRaw):dt;} function rootFromContract(contract){const m=String(contract||'').trim().toUpperCase().match(/^[A-Z]+\d*/);if(!m) return 'UNKNOWN';return m[0].replace(/\d+$/,'');} function normalizeSide(v){const s=String(v||'').toLowerCase();if(s.startsWith('b')) return 'BUY'; if(s.startsWith('s')) return 'SELL'; return 'UNKNOWN';} function getHourBlock(d){const h=d.getHours(),n=(h+1)%24;return `${String(h).padStart(2,'0')}:00-${String(n).padStart(2,'0')}:00`;}
function parseFills(rawRows){return rawRows.map((row,idx)=>{const dt=parseDateTime(row);const contract=findValue(row,['contract','symbol','instrument']);const root=rootFromContract(contract);const side=normalizeSide(findValue(row,['b_s','side','buy_sell']));const price=parseNumber(findValue(row,['avgprice','avg_fill_price','price','avg_fill']));const qty=parseNumber(findValue(row,['filledqty','quantity','qty','filled_qty']));return {id:idx+1,dt,date:dt.toISOString().slice(0,10),time:String(findValue(row,['fill_time','time'])||'').slice(0,8),contract,root,side,price,qty,hourBlock:getHourBlock(dt)};}).filter(r=>r.contract&&r.side!=='UNKNOWN'&&r.qty>0&&Number.isFinite(r.price));}
function pairFills(fills){const books=new Map();const trades=[];const unknown=new Set();const commissionPerSide=parseNumber(els.commissionPerSide.value||0);const sorted=[...fills].sort((a,b)=>a.dt-b.dt);for(const fill of sorted){if(!books.has(fill.contract)) books.set(fill.contract,{long:[],short:[]});const book=books.get(fill.contract);let remaining=fill.qty;if(fill.side==='BUY'){while(remaining>0&&book.short.length){const open=book.short[0];const matched=Math.min(remaining,open.remaining);const spec=CONTRACT_SPECS[fill.root];if(!spec) unknown.add(fill.root);const gross=spec?((open.price-fill.price)*matched*spec.pointValue):0;const commission=matched*commissionPerSide*2;trades.push({contract:fill.contract,root:fill.root,direction:'SHORT',entryDate:open.dt.toISOString().slice(0,10),exitDate:fill.dt.toISOString().slice(0,10),entryTime:open.dt.toTimeString().slice(0,8),exitTime:fill.dt.toTimeString().slice(0,8),hourBlock:getHourBlock(open.dt),qty:matched,entryPrice:open.price,exitPrice:fill.price,grossPnl:gross,commission,pnl:gross-commission});open.remaining-=matched;remaining-=matched;if(open.remaining<=0) book.short.shift();}if(remaining>0) book.long.push({price:fill.price,remaining,dt:fill.dt});}
      if(fill.side==='SELL'){while(remaining>0&&book.long.length){const open=book.long[0];const matched=Math.min(remaining,open.remaining);const spec=CONTRACT_SPECS[fill.root];if(!spec) unknown.add(fill.root);const gross=spec?((fill.price-open.price)*matched*spec.pointValue):0;const commission=matched*commissionPerSide*2;trades.push({contract:fill.contract,root:fill.root,direction:'LONG',entryDate:open.dt.toISOString().slice(0,10),exitDate:fill.dt.toISOString().slice(0,10),entryTime:open.dt.toTimeString().slice(0,8),exitTime:fill.dt.toTimeString().slice(0,8),hourBlock:getHourBlock(open.dt),qty:matched,entryPrice:open.price,exitPrice:fill.price,grossPnl:gross,commission,pnl:gross-commission});open.remaining-=matched;remaining-=matched;if(open.remaining<=0) book.long.shift();}if(remaining>0) book.short.push({price:fill.price,remaining,dt:fill.dt});}}
    } return {trades,unknownRoots:[...unknown].sort()};}
function summarizeGroup(label,items){const wins=items.filter(r=>r.pnl>0),losses=items.filter(r=>r.pnl<0),total=items.reduce((s,r)=>s+r.pnl,0);return {label,trades:items.length,winRate:items.length?wins.length/items.length:0,totalPnl:total,avgTrade:items.length?total/items.length:0,expectancy:items.length?total/items.length:0};}
function aggregate(rows,key){const map=new Map();rows.forEach(r=>{const val=r[key]||'Unknown';if(!map.has(val)) map.set(val,[]);map.get(val).push(r);});return Array.from(map.entries()).map(([label,items])=>summarizeGroup(label,items));} function overallStats(rows){const totalPnl=rows.reduce((s,r)=>s+r.pnl,0),wins=rows.filter(r=>r.pnl>0).length,avgTrade=rows.length?totalPnl/rows.length:0,best=rows.length?Math.max(...rows.map(r=>r.pnl)):0,worst=rows.length?Math.min(...rows.map(r=>r.pnl)):0;return {totalPnl,wins,trades:rows.length,winRate:rows.length?wins/rows.length:0,avgTrade,best,worst};} function cumulativeSeries(rows){const sorted=[...rows].sort((a,b)=>(a.entryDate+a.entryTime).localeCompare(b.entryDate+b.entryTime));let running=0;return sorted.map(r=>{running+=r.pnl;return {label:`${r.entryDate} • ${r.contract}`,value:running};});} function fmtMoney(n){return `${n<0?'-':''}$${Math.abs(n).toFixed(2)}`;} function fmtPct(n){return `${(n*100).toFixed(1)}%`;}
function renderKpis(stats,leak){els.kpiNet.textContent=fmtMoney(stats.totalPnl);els.kpiNet.className=`value ${stats.totalPnl>0?'positive':stats.totalPnl<0?'negative':'neutral'}`;els.kpiNetSub.textContent=`Best ${fmtMoney(stats.best)} • Worst ${fmtMoney(stats.worst)}`;els.kpiWinRate.textContent=fmtPct(stats.winRate);els.kpiWinRateSub.textContent=`${stats.wins} winners / ${stats.trades} trades`;els.kpiExpectancy.textContent=fmtMoney(stats.avgTrade);els.kpiExpectancy.className=`value ${stats.avgTrade>0?'positive':stats.avgTrade<0?'negative':'neutral'}`;els.kpiExpectancySub.textContent='Average P&L per paired trade';els.kpiLeak.textContent=leak?leak.label:'None';els.kpiLeak.className=`value ${leak?'negative':''}`;els.kpiLeakSub.textContent=leak?`${fmtMoney(leak.expectancy)} expectancy across ${leak.trades} trades`:'No qualifying negative cluster';}
function buildTable(columns,rows){if(!rows.length) return `<div class="empty-state">No data for this view yet.</div>`;const thead=`<thead><tr>${columns.map(c=>`<th>${c.label}</th>`).join('')}</tr></thead>`;const tbody=`<tbody>${rows.map(row=>`<tr>${columns.map(c=>`<td class="${c.num?'num':''}">${c.render?c.render(row[c.key],row):row[c.key]}</td>`).join('')}</tr>`).join('')}</tbody>`;return `<table>${thead}${tbody}</table>`;}
function renderTables(contractRows,hourRows,unknownRoots,leakRows){const cols=[{key:'label',label:'Group'},{key:'trades',label:'Trades',num:true},{key:'winRate',label:'Win rate',num:true,render:v=>fmtPct(v)},{key:'avgTrade',label:'Avg trade',num:true,render:v=>fmtMoney(v)},{key:'totalPnl',label:'Total P&L',num:true,render:v=>fmtMoney(v)}];els.setupTableWrap.innerHTML=buildTable(cols,contractRows);els.hourlyTableWrap.innerHTML=buildTable(cols,hourRows);els.weekdayTableWrap.innerHTML=unknownRoots.length?`<table><thead><tr><th>Unknown root</th><th>Status</th></tr></thead><tbody>${unknownRoots.map(r=>`<tr><td class="num">${r}</td><td class="warn">Needs point value spec</td></tr>`).join('')}</tbody></table>`:'<div class="empty-state">All detected contract roots were priced automatically.</div>';els.leakTableWrap.innerHTML=buildTable(cols,leakRows);} 
function destroyCharts(){Object.values(state.charts).forEach(c=>c&&c.destroy());} function chartColors(){const css=getComputedStyle(document.documentElement);return {primary:css.getPropertyValue('--color-primary').trim(),success:css.getPropertyValue('--color-success').trim(),error:css.getPropertyValue('--color-error').trim(),muted:css.getPropertyValue('--color-text-muted').trim(),text:css.getPropertyValue('--color-text').trim()};} function baseChartOptions(palette){return {maintainAspectRatio:false,responsive:true,plugins:{legend:{labels:{color:palette.text,font:{family:'IBM Plex Sans',size:12}}},tooltip:{callbacks:{label:ctx=>`${ctx.dataset.label}: ${fmtMoney(ctx.parsed.y??ctx.parsed)}`}}},scales:{x:{ticks:{color:palette.muted,maxRotation:0,autoSkip:true,font:{family:'IBM Plex Sans',size:11}},grid:{display:false}},y:{ticks:{color:palette.muted,callback:value=>fmtMoney(value),font:{family:'IBM Plex Sans',size:11}},grid:{color:'rgba(255,255,255,.08)'}}}};} 
function renderCharts(trades,contractRows,hourRows){destroyCharts();const palette=chartColors();const equity=cumulativeSeries(trades);state.charts.equity=new Chart(document.getElementById('equityChart'),{type:'line',data:{labels:equity.map(d=>d.label),datasets:[{label:'Cumulative P&L',data:equity.map(d=>d.value),borderColor:palette.primary,backgroundColor:'rgba(79,152,163,.18)',borderWidth:2,fill:true,tension:.28}]},options:baseChartOptions(palette)});state.charts.hourly=new Chart(document.getElementById('hourlyChart'),{type:'bar',data:{labels:hourRows.map(d=>d.label),datasets:[{label:'Total P&L',data:hourRows.map(d=>d.totalPnl),backgroundColor:hourRows.map(d=>d.totalPnl>=0?'rgba(105,179,109,.7)':'rgba(219,111,124,.72)'),borderRadius:8}]},options:baseChartOptions(palette)});state.charts.setup=new Chart(document.getElementById('setupChart'),{type:'bar',data:{labels:contractRows.map(d=>d.label),datasets:[{label:'Total P&L',data:contractRows.map(d=>d.totalPnl),backgroundColor:contractRows.map(d=>d.totalPnl>=0?'rgba(79,152,163,.75)':'rgba(219,111,124,.72)'),borderRadius:8}]},options:baseChartOptions(palette)});} 
function renderSummary(trades,stats,contracts,hours,leaks,unknownRoots){const bestContract=[...contracts].sort((a,b)=>b.expectancy-a.expectancy)[0];const worstContract=[...contracts].sort((a,b)=>a.expectancy-b.expectancy)[0];const bestHour=[...hours].sort((a,b)=>b.expectancy-a.expectancy)[0];const worstHour=[...hours].sort((a,b)=>a.expectancy-b.expectancy)[0];const focus=els.strategyFocus.value.trim();const bullets=[`<div class="bullet"><strong>Net result:</strong> ${fmtMoney(stats.totalPnl)} across ${stats.trades} paired trades, with ${fmtPct(stats.winRate)} win rate and ${fmtMoney(stats.avgTrade)} expectancy.</div>`,bestContract?`<div class="bullet"><strong>Best contract:</strong> ${bestContract.label} contributed ${fmtMoney(bestContract.totalPnl)} over ${bestContract.trades} trades.</div>`:'',worstContract?`<div class="bullet"><strong>Worst contract:</strong> ${worstContract.label} printed ${fmtMoney(worstContract.totalPnl)} and ${fmtMoney(worstContract.expectancy)} expectancy per trade.</div>`:'',bestHour?`<div class="bullet"><strong>Best time block:</strong> ${bestHour.label} returned ${fmtMoney(bestHour.totalPnl)} total.</div>`:'',worstHour?`<div class="bullet"><strong>Weakest time block:</strong> ${worstHour.label} returned ${fmtMoney(worstHour.totalPnl)} and should be reviewed as a cutoff candidate.</div>`:'',leaks.length?`<div class="bullet"><strong>Flagged leaks:</strong> ${leaks.map(l=>`${l.label} (${fmtMoney(l.expectancy)} avg)`).join(', ')}.</div>`:'<div class="bullet"><strong>Leak scan:</strong> No qualifying negative cluster met the minimum sample size.</div>',unknownRoots.length?`<div class="bullet"><strong>Unknown contract specs:</strong> ${unknownRoots.join(', ')} need a point value added before their trades can be fully priced.</div>`:'',focus?`<div class="bullet"><strong>Review note:</strong> ${focus}</div>`:''].filter(Boolean).join('');els.summaryBullets.innerHTML=bullets;} 
function analyzeFills(fills){const paired=pairFills(fills);state.trades=paired.trades;state.unknownRoots=paired.unknownRoots;els.tradeCountStatus.textContent=`${paired.trades.length} paired trades`;els.unknownStatus.textContent=`${paired.unknownRoots.length} unknown specs`;const stats=overallStats(paired.trades);const contracts=aggregate(paired.trades,'root').sort((a,b)=>b.totalPnl-a.totalPnl);const hours=aggregate(paired.trades,'hourBlock').sort((a,b)=>a.label.localeCompare(b.label));const threshold=Math.max(1,Number(els.losingThreshold.value)||3);const leaks=[...contracts,...hours].filter(g=>g.trades>=threshold&&g.expectancy<0).sort((a,b)=>a.expectancy-b.expectancy);const leak=leaks[0]||null;renderKpis(stats,leak);renderSummary(paired.trades,stats,contracts,hours,leaks,paired.unknownRoots);renderTables(contracts,hours,paired.unknownRoots,leaks);renderCharts(paired.trades,contracts.slice(0,10),hours);} 
function handleCSVText(text,filename='CSV loaded'){try{const raw=parseCSV(text);const fills=parseFills(raw);if(!fills.length) throw new Error('No usable fill rows found. Make sure your CSV includes Date, Fill Time, Contract, B/S, avgPrice, and filledQty or equivalent headers.');state.fills=fills;els.fileStatus.textContent=filename;analyzeFills(fills);}catch(err){els.summaryBullets.innerHTML=`<div class="bullet"><strong>Import error:</strong> ${err.message}</div>`;}}
els.csvFileInput.addEventListener('change',e=>{const file=e.target.files[0];if(!file) return;const reader=new FileReader();reader.onload=()=>handleCSVText(reader.result,file.name);reader.readAsText(file);});els.downloadSampleBtn.addEventListener('click',()=>{const blob=new Blob([sampleCSV],{type:'text/csv;charset=utf-8;'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='sample-tradovate-fills.csv';a.click();URL.revokeObjectURL(url);});els.themeToggle.addEventListener('click',()=>{state.theme=document.documentElement.getAttribute('data-theme')==='dark'?'light':'dark';document.documentElement.setAttribute('data-theme',state.theme);if(state.trades.length) analyzeFills(state.fills);});els.losingThreshold.addEventListener('change',()=>{if(state.fills.length) analyzeFills(state.fills);});els.commissionPerSide.addEventListener('change',()=>{if(state.fills.length) analyzeFills(state.fills);});handleCSVText(sampleCSV,'sample-tradovate-fills.csv');
</script>
</body></html>
